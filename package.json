{
  "name": "camerongoodman.github.io",
  "version": "1.0.0",
  "private": true,
  "description": "Personal site built with Hugo and the PaperMod theme",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "npm@12.0.1",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify"
  }
}
